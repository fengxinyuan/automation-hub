# 自动签到系统环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ========== 站点账号配置 ==========
# 可选：如果不想在 YAML 中存储明文密码，可以使用环境变量
# 格式：SITE_<站点名>_<账号索引>_PASSWORD
# 示例：
# SITE_ANYROUTER_0_PASSWORD=your-password-here
# SITE_ANYROUTER_1_PASSWORD=another-password

# ========== AI 配置（Linux.do 专用，可选）==========
# 如果不配置，将使用简单的文本提取和热度排序
# 配置后可启用 AI 智能总结和个性化推荐功能

# ===== 推荐：阿里云通义千问 qwen-flash（速度快、成本低）=====
# 获取 API Key：https://dashscope.console.aliyun.com/apiKey
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# API 端点（默认值，通常无需修改）
AI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# 使用的模型（默认 qwen-flash）
# 可选：qwen-flash, qwen-plus, qwen-max, qwen-turbo
AI_MODEL=qwen-flash

# 模型参数
AI_TEMPERATURE=0.7  # 创造性（0-1，越高越有创造性）
AI_MAX_TOKENS=800   # 最大生成长度

# ===== 备选：OpenAI（如果使用 OpenAI API）=====
# 获取方式：https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1  # 可选
# AI_MODEL=gpt-3.5-turbo  # 或 gpt-4, gpt-4-turbo

# ===== 备选：其他兼容 OpenAI API 的服务 =====
# AI_API_KEY=your-api-key
# AI_API_BASE=https://your-ai-service.com/v1
# AI_MODEL=your-model-name

# ========== 邮件通知配置 ==========
# 启用邮件通知
EMAIL_ENABLED=true

# SMTP 服务器配置
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_USE_TLS=true

# 收件人列表（用逗号分隔）
EMAIL_TO_ADDRESSES=notify1@example.com,notify2@example.com

# ========== 日志配置 ==========
LOG_LEVEL=INFO
LOG_FILE=logs/checkin.log

# ========== 浏览器配置 ==========
# 是否使用无头模式
HEADLESS=true
# 浏览器超时时间（毫秒）
PAGE_LOAD_TIMEOUT=30000

# ========== 并发配置 ==========
# 最大并发任务数
MAX_CONCURRENT_TASKS=2
# 账号之间延迟范围（秒）
MIN_DELAY_BETWEEN_ACCOUNTS=2
MAX_DELAY_BETWEEN_ACCOUNTS=5
